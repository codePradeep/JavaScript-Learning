let x = 5;
let y = 5;
const myObj = {firstname:"John", lastname:"Doe"};
console.assert(x + y == 11, myObj.firstname);

// The assert() method writes a message to the console if an expression evaluates to false